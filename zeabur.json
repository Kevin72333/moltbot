{
  "build": {
    "prepare_command": "apt update && apt install -y build-essential curl file git procps && /bin/bash -c \"$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)\" && test -d /home/linuxbrew/.linuxbrew && eval \"$(/home/linuxbrew/.linuxbrew/bin/brew shellenv)\" && brew install go"
  },
  "start": {
    "command": "node index.js"
  }
}
